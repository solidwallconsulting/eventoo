<form method="post" action="{{ path('stand_videos_delete', {'id': stand_video.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stand_video.id) }}">
    <button class="btn btn-danger">{% trans into app.session.get('lng') %}Supprimer{% endtrans %}</button>
</form>
