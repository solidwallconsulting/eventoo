{% extends 'base.html.twig' %}

{% block title %}EventsAccessTypes{% endblock %}

{% block body %}
    <h1>EventsAccessTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ events_access_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ events_access_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('events_access_types_index') }}">back to list</a>

    <a href="{{ path('events_access_types_edit', {'id': events_access_type.id}) }}">edit</a>

    {{ include('events_access_types/_delete_form.html.twig') }}
{% endblock %}
