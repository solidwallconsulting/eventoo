<form method="post" action="{{ path('key_words_delete', {'id': event_room.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_room.id) }}">
    <button class="btn">Delete</button>
</form>
