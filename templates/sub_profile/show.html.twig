{% extends 'base.html.twig' %}

{% block title %}SubProfile{% endblock %}

{% block body %}
    <h1>SubProfile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sub_profile.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sub_profile.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sub_profile_index') }}">back to list</a>

    <a href="{{ path('sub_profile_edit', {'id': sub_profile.id}) }}">edit</a>

    {{ include('sub_profile/_delete_form.html.twig') }}
{% endblock %}
