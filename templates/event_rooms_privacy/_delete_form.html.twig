<form method="post" action="{{ path('event_rooms_privacy_delete', {'id': event_rooms_privacy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_rooms_privacy.id) }}">
    <button class="btn">Delete</button>
</form>
