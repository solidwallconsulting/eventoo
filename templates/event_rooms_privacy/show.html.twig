{% extends 'base.html.twig' %}

{% block title %}EventRoomsPrivacy{% endblock %}

{% block body %}
    <h1>EventRoomsPrivacy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_rooms_privacy.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ event_rooms_privacy.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('event_rooms_privacy_index') }}">back to list</a>

    <a href="{{ path('event_rooms_privacy_edit', {'id': event_rooms_privacy.id}) }}">edit</a>

    {{ include('event_rooms_privacy/_delete_form.html.twig') }}
{% endblock %}
