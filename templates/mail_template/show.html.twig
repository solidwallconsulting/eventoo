{% extends 'base.html.twig' %}

{% block title %}MailTemplate{% endblock %}

{% block body %}
    <h1>MailTemplate</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mail_template.id }}</td>
            </tr>
            <tr>
                <th>Object</th>
                <td>{{ mail_template.object }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ mail_template.content }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ mail_template.name }}</td>
            </tr>
            <tr>
                <th>Profiles</th>
                <td>{{ mail_template.profiles ? mail_template.profiles|join(', ') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mail_template_index') }}">back to list</a>

    <a href="{{ path('mail_template_edit', {'id': mail_template.id}) }}">edit</a>

    {{ include('mail_template/_delete_form.html.twig') }}
{% endblock %}
