{% extends 'base.html.twig' %}

{% block title %}EventsAccessibility{% endblock %}

{% block body %}
    <h1>EventsAccessibility</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ events_accessibility.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ events_accessibility.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('events_accessibility_index') }}">back to list</a>

    <a href="{{ path('events_accessibility_edit', {'id': events_accessibility.id}) }}">edit</a>

    {{ include('events_accessibility/_delete_form.html.twig') }}
{% endblock %}
