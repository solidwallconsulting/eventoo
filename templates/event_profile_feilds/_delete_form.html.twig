<form method="post" action="{{ path('event_profile_feilds_delete', {'id': event_profile_feild.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_profile_feild.id) }}">
    <button class="btn btn-danger btn-sm ">{% trans into app.session.get('lng') %}supprimer{% endtrans %}</a></button>
</form>
