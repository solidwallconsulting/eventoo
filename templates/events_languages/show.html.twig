{% extends 'base.html.twig' %}

{% block title %}EventsLanguages{% endblock %}

{% block body %}
    <h1>EventsLanguages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ events_language.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ events_language.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('events_languages_index') }}">back to list</a>

    <a href="{{ path('events_languages_edit', {'id': events_language.id}) }}">edit</a>

    {{ include('events_languages/_delete_form.html.twig') }}
{% endblock %}
