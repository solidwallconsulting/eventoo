<form method="post" action="{{ path('meeting_sessions_delete', {'id': meeting_session.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ meeting_session.id) }}">
    <button class="btn">Delete</button>
</form>
