{% extends 'base.html.twig' %}

{% block title %}Exposer{% endblock %}

{% block body %}
    <h1>Exposer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exposer.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ exposer.name }}</td>
            </tr>
            <tr>
                <th>Website</th>
                <td>{{ exposer.website }}</td>
            </tr>
            <tr>
                <th>LogoURL</th>
                <td>{{ exposer.logoURL }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('exposer_index') }}">back to list</a>

    <a href="{{ path('exposer_edit', {'id': exposer.id}) }}">edit</a>

    {{ include('exposer/_delete_form.html.twig') }}
{% endblock %}
