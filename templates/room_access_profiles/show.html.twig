{% extends 'base.html.twig' %}

{% block title %}RoomAccessProfiles{% endblock %}

{% block body %}
    <h1>RoomAccessProfiles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ room_access_profile.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('room_access_profiles_index') }}">back to list</a>

    <a href="{{ path('room_access_profiles_edit', {'id': room_access_profile.id}) }}">edit</a>

    {{ include('room_access_profiles/_delete_form.html.twig') }}
{% endblock %}
