{% extends 'base.html.twig' %}

{% block title %}EventProfileFeildValue{% endblock %}

{% block body %}
    <h1>EventProfileFeildValue</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_profile_feild_value.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ event_profile_feild_value.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('event_profile_feild_value_index') }}">back to list</a>

    <a href="{{ path('event_profile_feild_value_edit', {'id': event_profile_feild_value.id}) }}">edit</a>

    {{ include('event_profile_feild_value/_delete_form.html.twig') }}
{% endblock %}
