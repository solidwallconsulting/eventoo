<form method="post" action="{{ path('event_profile_feild_value_delete', {'id': event_profile_feild_value.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_profile_feild_value.id) }}">
    <button class="btn">Delete</button>
</form>
