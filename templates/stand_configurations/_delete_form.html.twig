<form method="post" action="{{ path('stand_configurations_delete', {'id': stand_configuration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stand_configuration.id) }}">
    <button class="btn">Delete</button>
</form>
