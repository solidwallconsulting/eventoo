{% extends 'base.html.twig' %}

{% block title %}SponsorsTypes{% endblock %}

{% block body %}
    <h1>SponsorsTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sponsors_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sponsors_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sponsors_types_index') }}">back to list</a>

    <a href="{{ path('sponsors_types_edit', {'id': sponsors_type.id}) }}">edit</a>

    {{ include('sponsors_types/_delete_form.html.twig') }}
{% endblock %}
