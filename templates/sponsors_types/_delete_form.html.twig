<form method="post" action="{{ path('sponsors_types_delete', {'id': sponsors_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sponsors_type.id) }}">
     
</form>
