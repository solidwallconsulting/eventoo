<form method="post" action="{{ path('stand_product_delete', {'id': stand_product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stand_product.id) }}">
    <button class="btn btn-danger">{% trans into app.session.get('lng') %}Supprimer{% endtrans %}</button>
</form>
