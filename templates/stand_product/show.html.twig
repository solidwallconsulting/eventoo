{% extends 'base.html.twig' %}

{% block title %}StandProduct{% endblock %}

{% block body %}
    <h1>StandProduct</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stand_product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stand_product.name }}</td>
            </tr>
            <tr>
                <th>Descreption</th>
                <td>{{ stand_product.descreption }}</td>
            </tr>
            <tr>
                <th>PhotoURL</th>
                <td>{{ stand_product.photoURL }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stand_product_index') }}">back to list</a>

    <a href="{{ path('stand_product_edit', {'id': stand_product.id}) }}">edit</a>

    {{ include('stand_product/_delete_form.html.twig') }}
{% endblock %}
