{% extends 'base.html.twig' %}

{% block title %}EventsDurations{% endblock %}

{% block body %}
    <h1>EventsDurations</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ events_duration.id }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ events_duration.duration }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('events_durations_index') }}">back to list</a>

    <a href="{{ path('events_durations_edit', {'id': events_duration.id}) }}">edit</a>

    {{ include('events_durations/_delete_form.html.twig') }}
{% endblock %}
