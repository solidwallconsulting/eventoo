<form method="post" action="{{ path('event_profiles_delete', {'id': event_profile.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_profile.id) }}">
    <button class="btn">Delete</button>
</form>
