{% extends 'base.html.twig' %}

{% block title %}EventTypes{% endblock %}

{% block body %}
    <h1>EventTypes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ event_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('event_types_index') }}">back to list</a>

    <a href="{{ path('event_types_edit', {'id': event_type.id}) }}">edit</a>

    {{ include('event_types/_delete_form.html.twig') }}
{% endblock %}
