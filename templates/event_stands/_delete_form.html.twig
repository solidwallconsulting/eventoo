<form method="post" action="{{ path('event_stands_delete', {'id': event_stand.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_stand.id) }}">
    <button class="btn">Delete</button>
</form>
